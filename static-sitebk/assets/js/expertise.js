/* ============================================
   EXPERTISE.JS — Expertise page
   The Expertise page is a static content page with
   no tabs, filters, or interactive elements. Scroll-reveal
   animations are handled globally by global.js.
   This file is included for consistency with the project's
   page-specific JS convention and is intentionally minimal.
   ============================================ */

(function () {
  "use strict";

  document.addEventListener("DOMContentLoaded", function () {
    // No page-specific interactions required.
    // Scroll-reveal is handled by global.js via IntersectionObserver.
  });
})();
